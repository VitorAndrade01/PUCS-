#include <stdio.h>

int main() {
    long long int produto = 1;

    for (int i = 1; i <= 15; i += 2) {
        produto *= i;
    }

    printf("Produto dos ímpares de 1 a 15: %lld\n", produto);

    return 0;
}
