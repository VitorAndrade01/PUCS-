#include <stdio.h>
#include <math.h>

int main() {
    unsigned long long int total = 0, graos = 1;

    for (int i = 1; i <= 64; i++) {
        total += graos;
        graos *= 2;
    }

    printf("Total de grãos de trigo recebidos: %llu\n", total);

    return 0;
}
